{
  "name": "webhook-agent",
  "version": "1.0.0",
  "description": "Webhook listener agent — runs on each VPS",
  "main": "webhook.js",
  "scripts": {
    "start": "node webhook.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "body-parser": "^1.20.0"
  }
}
